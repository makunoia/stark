import React, { FC } from "react";
declare const ModalWrapper: FC<{
    show: boolean;
    children: React.ReactNode;
    onClick: React.MouseEventHandler<HTMLDivElement>;
}>;
export default ModalWrapper;
