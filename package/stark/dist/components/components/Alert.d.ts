import React from "react";
import { AlertProps } from "stark-types";
declare const Alert: ({ id, title, message, variant, action, onClose, icon, duration, }: AlertProps) => React.JSX.Element;
export default Alert;
