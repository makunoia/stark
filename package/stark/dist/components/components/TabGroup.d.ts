import React from "react";
import type { TabGroupProps } from "stark-types";
declare const TabGroup: ({ tabs, type, activeTab, handleTabClick, }: TabGroupProps) => React.JSX.Element;
export default TabGroup;
