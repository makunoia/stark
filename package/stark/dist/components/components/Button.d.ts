import React from "react";
import { ButtonProps } from "stark-types";
declare const Button: ({ label, id, leadingIcon, trailingIcon, variant, color, size, disabled, isLoading, fullWidth, submit, attached, onClick, className, }: ButtonProps) => React.JSX.Element;
export default Button;
