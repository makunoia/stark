{
  "name": "@repo/stark",
  "scripts": {
    "dev:sandbox": "turbo dev --filter=sandbox",
    "storybook": "turbo run storybook --filter=storybook"
  },
  "dependencies": {
    "@makunoia/stark": "*"
  },
  "workspaces": [
    "apps/*",
    "package/*"
  ]
}
